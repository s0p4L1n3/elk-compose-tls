output {

    #elasticsearch {
    #    hosts => "elasticsearch:9200"
    #    manage_template => false
    #    index => "stormshield-%{internal_product}-%{type}-%{+dd.MM.YYYY}"
    #    user => "logstash_internal"
    #    password => "${LOGSTASH_INTERNAL_PASSWORD}"
    #}

# test RFC5424 log format before sending it to elasticsearch
    stdout {}
}
